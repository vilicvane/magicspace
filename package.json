{
  "name": "magicspace-packages",
  "private": true,
  "repository": "https://github.com/makeflow/magicspace.git",
  "license": "MIT",
  "author": "Chengdu Mufan Technology Co., Ltd.",
  "type": "module",
  "scripts": {
    "3": "npm install && npm dedupe && npm install",
    "build": "rimraf --glob packages/*/bld && tsc --build",
    "lint": "eslint --no-error-on-unmatched-pattern --report-unused-disable-directives .",
    "lint-prettier": "prettier --check .",
    "bare-test": "cross-env NODE_OPTIONS=--experimental-vm-modules jest",
    "test": "npm run lint-prettier && npm run build && npm run lint && npm run bare-test",
    "boilerplate-command": "npm run --workspace @magicspace/boilerplate-command",
    "boilerplate-url": "npm run --workspace @magicspace/boilerplate-url",
    "boilerplate-url-resolver": "npm run --workspace @magicspace/boilerplate-url-resolver",
    "core": "npm run --workspace @magicspace/core",
    "utils": "npm run --workspace @magicspace/utils",
    "magicspace": "npm run --workspace magicspace"
  },
  "workspaces": [
    "packages/boilerplate-command",
    "packages/boilerplate-url",
    "packages/boilerplate-url-resolver",
    "packages/core",
    "packages/utils",
    "packages/cli"
  ],
  "devDependencies": {
    "@mufan/eslint-plugin": "^0.3.0",
    "@types/jest": "^30.0.0",
    "cross-env": "^10.1.0",
    "eslint": "^10.2.0",
    "jest": "^30.3.0",
    "prettier": "^3.8.3",
    "rimraf": "^6.1.3",
    "typescript": "^6.0.2"
  }
}
