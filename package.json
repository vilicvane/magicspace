{
  "name": "magicspace",
  "private": true,
  "scripts": {
    "lerna:publish": "lerna publish --npm-client=npm --registry=https://registry.npmjs.org",
    "test:tslint-rules": "yarn workspace @magicspace/tslint-rules test",
    "test": "yarn test:tslint-rules"
  },
  "devDependencies": {
    "lerna": "^2.11.0",
    "prettier": "^1.13.7",
    "rimraf": "^2.6.2",
    "tslint": "^5.11.0",
    "tslint-language-service": "^0.9.9",
    "typescript": "^3.0.1"
  },
  "workspaces": {
    "packages": [
      "tslint-rules",
      "configs"
    ]
  }
}
